# 公开科普与路线入库验证

日期：2026-09-23。范围：本机 PostgreSQL、真实 HTTP 接口、本地微信开发者工具预览。服务器未部署本批数据；未代用户上传或提审。

## 内容与采集

| 内容 | 本次新增 | 本机公开总量 |
| --- | ---: | ---: |
| 科普手记 | 30 | 38 |
| 漫步路线 | 30 | 31 |
| 京津地点 | 70 | 82（含原 12 个示范地点） |

科普分类：植物 8、水环境 7、绿色生活 8、生态智游 7。路线：天津 15 条/34 个地点，北京 15 条/36 个地点。60 项内容分别依据 60 个实际打开核读的来源页面整理；条目附机构、原文地址和查阅日期。没有复制原文整篇或图片，没有编造距离、时长、坐标、实时开放信息或高校入校权限。

采集清单：[植物与水环境](科普采集part1.md)、[绿色生活与智游](科普采集part2.md)、[京津漫步路线](漫步路线采集记录.md)。资料包和维护说明见 [公开内容 README](../../backend/knowledge/data/README.md)。

## 导入与数据保护

1. 入库前导出原有区域、地点、文章、路线和节点，备份在 `.runtime/content-backups/before-public-catalogue-20260923.json`，不进 Git。
2. 应用 `ecology.0003_place_landmark_kind`，补充“地标与停留点”分类；没有改写现有分类数据。
3. 预览确认新增 2 个真实区域、70 个地点、30 篇文章和 30 条路线，无身份冲突。
4. `import_public_knowledge --apply` 一次事务写入成功，新增 70 个路线节点。
5. 再次预览各类新增均为 0；原有 28 条记录与备份逐条比较完全一致。
6. 真实地点不关联示意地图、不添加坐标或河湖监测；原有模拟区域和观测继续保留原身份。

导入器已测试：只预览不写入；取消发布、修改正文、删除路线节点后重复导入仍保留修改；地点/区域冲突终止；写入中途异常整批回滚；拒绝同名假重复地点、换序重复路线、无来源、未来日期、虚构坐标和跨区域节点。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| 知识与导入后端测试 | 34 项通过（SQLite 隔离数据库） |
| 生态后端回归 | 73 项执行，69 通过、4 项 PostgreSQL 锁测试在 SQLite 跳过 |
| 小程序回归 | 273 项通过 |
| 微信原生编译 | 22 个 WXML、23 个 WXSS 通过 |
| 数据库迁移检查 | 无遗漏迁移 |
| PostgreSQL + HTTP 全量内容检查 | 新增 60 项均能打开详情；38 篇文章分 6 页、31 条路线分 5 页读取完整（每页 7 条）；区域过滤、节点数量、来源和无坐标状态正确 |
| 原有科普业务联调 | 真实 HTTP 检查通过：38 篇手记分 19 页加载，分类/植物/搜索组合、关联文章、原 6 节点路线及返回状态正常 |
| 原有数据保护 | 原有 28 条区域/地点/文章/路线/节点记录均未修改 |

全量检查摘要保存在 [内容联调结果](公开科普与路线接口结果.json)。本机详细日志在 `.runtime/content-*.log`、`.runtime/public-catalogue-smoke.json`。

## 展示与边界

- “漫步路线”增加“浏览全部区域路线”，避免首页所选示范校园把京津路线挡在筛选之外；选择全部区域不会改掉全局区域选择。
- 导览增加“步道地标”类型；没有底图的真实区域使用地点列表，修复空地图被误写为“图上已标注”的情况。
- 路线列表收起较长说明和来源，详情仍完整显示；原生检查发现长来源 URL 溢出后，已增加自动折行并重新通过原生编译和详情回归。
- 全量接口检查后紧接着运行旧业务联调，曾触发本机匿名请求限流。保留限流，等待窗口自然恢复后业务联调已通过，微信文章详情重试后也正常加载；没有关闭保护或修改额度。
- 本批未主动发送 AI 问题；资料采集和数据库导入不消耗 DeepSeek 或和风请求。刷新微信预览会按原有天气缓存与预算机制更新首页天气，没有调整预算。

## 微信开发者工具抽查

已在当前 `HYHQ-local-preview` 工程实际确认：

- 科普列表显示新增文章的分类、摘要与来源。
- “一片湖泊，不是宠物的新地址”详情的三段 Markdown 标题、正文与来源正常渲染。
- 从示范校园的“漫步路线”点“浏览全部区域路线”后，出现天津与北京的新路线。
- 打开“中新友好花园：认识雨水花园的思路”，显示来源、3 个真实节点、逐站浏览和地点详情入口。

以上是开发者工具本地预览抽查，不代表手机、登录或正式公网验收完成。

## 后续部署

本批已写入本机。服务器后续更新源码后需先备份，再执行迁移、导入预览与 `--apply`；仅拉 Git 不会自动更新文章表。暂无定时采集、自动内容审核或动态路线导航。

微信当前打开的 `miniprogram-preview` 只连接本机，仅供看页面；观察到工具上有“代码上传成功”提示，但本任务没有操作上传。正式上传应使用 `miniprogram` 目录；本地预览包不应提交审核。公网部署与提审仍受原有备案/HTTPS/微信配置准备条件约束。
