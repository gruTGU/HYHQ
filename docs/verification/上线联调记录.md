# 2026-09-23 正式域名与微信接口联调记录

基线：`19e3213`。本轮切换客户端目标并整理接口申请材料；服务器业务代码和数据库保持既有版本。

## 已完成

- [x] 确认 `greatdata.asia` 的 A 记录为 `81.70.40.241`。
- [x] 客户端默认 `https://greatdata.asia/api/v1`，关闭开发模拟登录；超时和上传大小限制保持原值。
- [x] 本机私有项目配置启用 `urlCheck: true`，保留真实 AppID 且继续被 Git 忽略；公共模板不包含真实密钥。
- [x] 257 项 Node 小程序测试通过，使用 mock，没有请求付费 API；配置及文档格式检查通过。
- [x] 服务器 Nginx、API、花卉、河道、LLM 服务及 Certbot timer 为 active，内部健康接口 `status=ok`。
- [x] 仅核对凭据存在状态：服务器 AppID 与本机匹配，AppSecret 为空，DeepSeek/和风凭据已配置；未输出任何 Secret。
- [x] 核对实际定位调用仅 `wx.getLocation`；没有实现 `wx.chooseLocation`。完成 [接口申请填写说明](../微信接口申请填写说明.md)。

## HTTPS 核查结果

公网根路径曾返回 HTTPS 跳转，但这不能证明整个域名已放行。证书验证路径的测试结果：

- 服务器本机通过域名或回环地址读取临时 ACME 文件：HTTP 200。
- 从开发机外网直连域名、或强制解析到该 IP：HTTP 302，跳转腾讯云 `webblock`。
- 一次 Let's Encrypt staging 验证收到拦截页并失败。未请求正式签发，当前没有正式证书或到期日；仅产生 staging 账户元数据。
- Nginx 配置没有修改或 reload；业务数据库、费用账本、API 密钥和其他服务没有变更。临时挑战文件已清理。
- 操作前私有备份：`/var/backups/hyhq/https-setup-20260923T133120Z`。脱敏核查结果留在本机被忽略的 `.runtime/https-preflight-20260923.json`。

用户已告知小程序备案成功；域名对应的网站备案/接入状态仍需核实，不能由小程序备案推定公网已放行。

## 下一步 TODO

- [ ] **P01 域名放行**：核实腾讯云网站备案/接入状态，确认公网 ACME 测试路径可用。
- [ ] **P02 HTTPS**：签发证书、配置 443 和 80 跳转、验证证书链与自动续期；不通过关闭证书校验继续联调。
- [ ] **P03 微信登录**：通过私有环境配置 AppSecret，验证真实 `wx.login → code2Session` 登录；不在客户端或 Git 保存凭据。
- [ ] **P04 合法域名**：用户在后台保存三类 `https://greatdata.asia` 域名，并在开发者工具/手机中校验生效。
- [ ] **P05 接口与隐私**：核对个人主体和服务类目是否可申请 getLocation，填写实际隐私项；补齐运营者、联系方式和正式说明。
- [ ] **P06 真实场景材料**：核对真实水体和坐标，完成实际定位、拒绝与不保存位置测试，录制真实截图/视频。
- [ ] **P07 完整联调与发布**：手机验证浏览、登录、上传、花卉/河道识别、三场景 AI、个人记录和注销后，再上传体验版、提审与发布。

本次浏览器工具不允许访问微信公众平台后台；本报告供用户自行填写，不包含后台提交操作。上列未完成项保持待办，不以本机自动测试替代正式上线验收。
