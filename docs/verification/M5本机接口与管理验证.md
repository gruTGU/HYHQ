# M5 本机接口与管理验证

## 结论

2026-09-24 10:26（北京时间），在完成备份和迁移、重启本地服务后，对 `http://127.0.0.1:8000` 和现有 PostgreSQL 业务库进行实际 HTTP 验证，首次 **65 项检查通过**；修复健康接口旧版本标识并重启 API 后，追加核对通过，合计 **66 项检查通过，0 项失败**。

本记录覆盖接口行为、管理页面加载和河道观察运行配置。微信开发者工具页面验证、隔离测试数据库的全量测试由另外的验证记录说明；本轮没有部署或推送代码。

机器可读结果保存在本机忽略目录 `.runtime/m5-return/http-report.json`。该文件只记录状态码、布尔断言、安全计数和公开接口路径，不保存登录凭据、会话 Cookie、管理列表行内容或用户原始资料。

## 实际验证结果

| 项目 | 操作与结果 |
| --- | --- |
| 健康检查 | `/api/v1/health/` 返回 200、`status=ok`，识别、河道观察与 LLM 配置状态可读。没有发起大模型对话。 |
| 旧公开入口 | 区域、地点、地图、监测站、指标、数据来源、河湖、科普标签、手记和路线列表均返回 200。 |
| 关键词检索 | “向日葵”返回 3 条已发布手记，结果类型为资料摘录。 |
| 植物标签 | `plant_label=sunflowers` 返回 2 条手记，结果均匹配标签。 |
| 科普分类 | `category=water` 返回 8 条资料，结果均为水资源分类。 |
| 地点筛选 | 选择一处现有公开地点，返回 3 条相关资料，覆盖手记、地点和路线；地点结果 ID 与筛选一致。 |
| 跨类别检索 | “水”检索到 48 份资料，首批结果同时覆盖手记、地点、路线，以及植物、水资源、绿色生活、文旅分类。 |
| 区域与分页 | 对现有区域分页读取，合并结果包含手记、地点和路线，接口分页状态正常。 |
| 无证据 | 不存在的组合关键词返回 `count=0`、空结果和 `answer_kind=no_evidence`，没有生成无依据回答。 |
| 参数校验 | 空查询、未知参数、重复参数、不支持分类、分类与地点类型冲突、页码 0、每页超限、非法植物标签、超长关键词、不存在的地点均返回 400。 |
| 来源跳转 | 从检索结果的本站来源路径打开手记、地点和路线详情，均返回 200，详情 ID 与检索结果一致。没有请求外站来源网页。 |
| 评论与举报 | 公开状态为关闭；公开评论读取返回 403，错误码为 `COMMUNITY_DISABLED`；个人评论、个人举报匿名访问均返回 401。 |
| 既有私有记录 | 河道观察任务、图片识别任务的匿名列表访问均返回 401。 |
| 天气预报 | 地点目录显示 `forecast_enabled=false`；天津预报入口返回 200，`enabled=false`、`data=null`、`reason=forecast_disabled`，没有调用天气上游。 |
| 天气订阅 | 状态入口为关闭、一次提醒模式、空模板和空用户提醒列表，未开放授权与发送。 |
| 无音频场景 | 对现有已发布手记及路线查询语音，均正常返回 200 和 `data=null`；非法/双目标参数返回 400，不存在的音频返回 404。 |

## 管理页面

通过本机已有管理员凭据完成真实登录，然后仅打开列表页；凭据从本机私有文件直接读取，没有输出或写入报告。

以下 **15 个管理页面全部返回 200**，没有跳回登录页，页面主体成功呈现：

- 评论与举报开放配置、评论、举报。
- 资料语音。
- 天气地点、持久缓存、月度请求账本、上游请求记录、一次天气提醒、提醒尝试记录。
- 科普内容、路线。
- 审计日志、任务日志、管理统计。

本轮没有使用发布、审核、删除、撤下、重试发送等管理操作，也没有记录列表中的私人内容。

## 河道观察运行检查

只读运行 `check_assessment_runtime`，得到 `status=ready`：

- NumPy、ONNX、ONNX Runtime CPU、图像库及进程控制依赖就绪。
- 模型文件校验和、计算图输入输出约定通过。
- 当前登记模型支持的目标类别数为 1。
- 活跃规则及工作进程配置检查通过。
- `inference_executed=false`，`queue_consumed=false`。

当前业务库的河道观察任务数为 0，没有可用于补查的成功历史任务。本轮没有创建新任务，因此上述结论是**模型与运行配置检查通过**，不代表本轮执行过图片推理。

## 数据与费用边界

检查前后，公开内容、路线、地点、评论、举报、语音、提醒、河道观察任务、识别任务及 LLM 轮次的记录数量保持一致。此处只比较数量，不以数量比较代替逐行内容审计。

天气上游记录数 **42 → 42**，月度累计预留请求数 **42 → 42**。本轮未访问可能刷新天气的 `/weather-data/summary/`，没有调用 DeepSeek、微信内容安全或订阅发送服务。

关闭预报入口的首次读取按既有实现创建了 **1 条空预报缓存占位记录**，未保存预报数据、未预留天气请求。管理员登录会产生正常登录会话和登录时间更新，请求日志也可能产生系统记录；这些均属于已授权的本机验证过程。其他本机验证可能同时进行，计数差值只作观察，不归因于未跟踪的并发操作。

## 留意事项

首次实际读取发现健康接口 `version` 仍显示旧的 `m3`。已修复为 `m5`，同步现有健康接口断言；使用隔离 SQLite 测试库运行 `recognition.tests.RegistryWorkerTests.test_health_contract_and_missing_artifact`，1 项通过，没有连接业务库执行测试。随后仅重启 API，再次实际 GET 返回 200、`status=ok`、`version=m5`。原始 65 项结果与第 66 项修复后核对均保留在本机报告中。

未开放的评论、预报和订阅仍保持关闭，未伪装成已上线。真实发送、真实付费取数、河道图片推理和正式环境部署不在本记录的验证范围内。
